<mvvm:BaseUserControl x:Class="DocumentPacker.Parts2.Main.FeaturesPart.FeaturesView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mvvm="clr-namespace:DocumentPacker.Mvvm"
                 xmlns:controls="clr-namespace:DocumentPacker.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock 
            Style="{StaticResource HeadlineTextBlockStyle}"
            Text="{Binding Headline}"/>

        <controls:CustomItemsControl
            Grid.Row="1"
            Grid.Column="0"
            ItemsSource="{Binding FeatureElements}">
            <controls:CustomItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button
                        Margin="16" 
                        Padding="16" 
                        VerticalContentAlignment="Stretch">
                        <Grid >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="200"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                FontSize="24"
                                FontWeight="Bold"
                                Margin="8"
                                Text="{Binding Headline}"
                                TextAlignment="Center"/>
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                VerticalAlignment="Stretch"
                                Text="{Binding Description}"
                                TextWrapping="Wrap"/>
                            <Image
                                Grid.Row="1"
                                Grid.Column="0"
                                Source="{Binding IconPath}"/>
                        </Grid>
                    </Button>
                </DataTemplate>
            </controls:CustomItemsControl.ItemTemplate>
        </controls:CustomItemsControl>
    </Grid>
</mvvm:BaseUserControl>
