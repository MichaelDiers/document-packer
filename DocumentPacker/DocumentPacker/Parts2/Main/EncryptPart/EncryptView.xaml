<mvvm:BaseUserControl x:Class="DocumentPacker.Parts2.Main.EncryptPart.EncryptView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mvvm="clr-namespace:DocumentPacker.Mvvm"
                 xmlns:encryptPart="clr-namespace:DocumentPacker.Parts2.Main.EncryptPart"
                 xmlns:dependencyProperties="clr-namespace:DocumentPacker.DependencyProperties"
                 xmlns:resources="clr-namespace:DocumentPacker.Resources">
    <mvvm:BaseUserControl.Resources>
        <encryptPart:EncryptItemTypeToStringConverter x:Key="EncryptItemTypeToStringConverter"/>
        <ControlTemplate x:Key="ValidationTemplate">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                    <Image ToolTip="{Binding Path=/ErrorContent}" Height="25" Width="25" Source="../../../Assets/material_symbol_error.png"/>
                    <TextBlock Foreground="Red" Text="{Binding Path=/ErrorContent}"/>
                    <StackPanel.RenderTransform>
                        <TranslateTransform X="16" Y="20"/>
                    </StackPanel.RenderTransform>
                </StackPanel>
                <AdornedElementPlaceholder  />
            </Grid>
        </ControlTemplate>

    </mvvm:BaseUserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Style="{StaticResource HeadlineTextBlockPrimaryStyle}"
            Text="{Binding Headline}"/>

        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            HorizontalAlignment="Center"
            Text="{Binding Description}"
            TextWrapping="Wrap"/>

        <Border
            Grid.Row="2"
            Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- document packer file/folder -->
                <Label 
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    Grid.Column="0"
                    Content="{x:Static resources:Translation.EncryptPartDocumentPackerFile}"/>
                <TextBox
                    Grid.Row="0"
                    Grid.Column="1"
                    IsEnabled="False"
                    IsReadOnly="True"
                    Text="{Binding DocumentPackerOutputFolder}"
                    ToolTip="{x:Static resources:Translation.DocumentPackerOutputFolderToolTip}"/>
                <Button
                    Grid.Row="0"
                    Grid.Column="2" 
                    Command="{Binding SelectDocumentPackerOutputFolderCommand}"
                    dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.SelectDocumentPackerOutputFolderCommand}"
                    dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_folder.png"
                    Style="{StaticResource SmallCommandButton}"/>

                <!-- document packer file -->
                <TextBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Text="{Binding DocumentPackerOutputFile}"
                    ToolTip="{x:Static resources:Translation.DocumentPackerOutputFileToolTip}"/>

                <!-- rsa public key -->
                <Label 
                    Grid.Row="2"
                    Grid.Column="0"
                    Content="{x:Static resources:Translation.EncryptPartRsaPublicKey}"/>
                <TextBox
                    Grid.Row="2"
                    Grid.Column="1"
                    MaxHeight="250"
                    VerticalScrollBarVisibility="Auto"
                    Text="{Binding RsaPublicKeyPem}"
                    ToolTip="{x:Static resources:Translation.EncryptPartRsaPublicKeyToolTip}"/>
                <Button
                    Grid.Row="2"
                    Grid.Column="2"
                    Command="{Binding GenerateRsaKeysCommand}"
                    dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartGenerateRsaKeys}"
                    dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_key.png"
                    Style="{StaticResource SmallCommandButton}"/>

                <!-- rsa private key -->
                <Label 
                    Grid.Row="3"
                    Grid.Column="0"
                    Content="{x:Static resources:Translation.EncryptPartRsaPrivateKey}"/>
                <TextBox
                    Grid.Row="3"
                    Grid.Column="1"
                    MaxHeight="250"
                    VerticalScrollBarVisibility="Auto"
                    Text="{Binding RsaPrivateKeyPem}"
                    ToolTip="{x:Static resources:Translation.EncryptPartRsaPrivateKeyToolTip}"/>
                <Button
                    Grid.Row="3"
                    Grid.Column="2"
                    Command="{Binding GenerateRsaKeysCommand}"
                    dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartGenerateRsaKeys}"
                    dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_key.png"
                    Style="{StaticResource SmallCommandButton}"/>

                <!-- items section-->
                <Grid
                    Grid.Row="4"
                    Grid.Column="0"
                    Grid.ColumnSpan="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- items headline -->
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Text="{x:Static resources:Translation.EncryptPartItemsHeadline}"
                        HorizontalAlignment="Center"/>

                    <!-- buttons -->
                    <StackPanel 
                        Grid.Row="1" 
                        Grid.Column="0" 
                        Orientation="Vertical">
                        <Button
                            Command="{Binding AddEncryptItemCommand}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartAddLabel}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_add.png"
                            Style="{StaticResource SmallCommandButton}"/>
                        <Button
                            Command="{Binding CollapseAllCommand}"
                            CommandParameter="{StaticResource True}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartCollapseAll}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_collapse_all.png"
                            Style="{StaticResource SmallCommandButton}"/>
                        <Button
                            Command="{Binding CollapseAllCommand}"
                            CommandParameter="{StaticResource False}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartExpandAll}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_unfold_more.png"
                            Style="{StaticResource SmallCommandButton}"/>
                    </StackPanel>

                    <!-- items -->
                    <ItemsControl 
                        Grid.Row="1"
                        Grid.RowSpan="3"
                        Grid.Column="1"
                        ItemsSource="{Binding EncryptItems}"
                        HorizontalAlignment="Stretch"
                        Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="encryptPart:EncryptItemViewModel">
                                <Expander 
                                    Background="DarkGray" 
                                    Header="{Binding Description}"
                                    IsExpanded="{Binding IsExpanded}"
                                    Padding="24">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Grid Grid.Column="0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- is required -->
                                            <Label
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Content="{x:Static resources:Translation.EncryptPartIsRequiredLabel}"/>
                                            <CheckBox
                                                Grid.Row="0"
                                                Grid.Column="1"
                                                Grid.ColumnSpan="2"
                                                IsChecked="{Binding IsRequired}"/>

                                            <!-- description -->
                                            <Label
                                                Grid.Row="1"
                                                Grid.Column="0"
                                                Content="{x:Static resources:Translation.EncryptPartDescriptionLabel}"/>
                                            <TextBox
                                                Grid.Row="1"
                                                Grid.Column="1"
                                                Grid.ColumnSpan="2"
                                                Text="{Binding Description}"
                                                TextWrapping="Wrap"/>
                                            
                                            <!-- item type -->
                                            <Label
                                                Grid.Row="2"
                                                Grid.Column="0"
                                                Content="{x:Static resources:Translation.EncryptPartDataTypeLabel}"/>
                                            <ComboBox
                                                Grid.Row="2"
                                                Grid.Column="1"
                                                Grid.ColumnSpan="2"
                                                x:Name="EncryptItemTypeComboBox"
                                                ItemsSource="{Binding EncryptItemTypes}"
                                                SelectedItem="{Binding EncryptItemType}"
                                                Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding .,Converter={StaticResource EncryptItemTypeToStringConverter}}"/>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>

                                            <!-- value -->
                                            <Label
                                                Grid.Row="3"
                                                Grid.Column="0"
                                                Content="{Binding ElementName=EncryptItemTypeComboBox, Path=SelectedValue, Converter={StaticResource EncryptItemTypeToStringConverter}, ConverterParameter={StaticResource True}}"/>
                                            <TextBox
                                                Grid.Row="3"
                                                Grid.Column="1"
                                                AllowDrop="True"
                                                Drop="OnDrop"
                                                PreviewDragOver="OnPreviewDragOver"
                                                Text="{Binding Value, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"
                                                TextWrapping="Wrap"
                                                Validation.ErrorTemplate="{StaticResource ValidationTemplate}">
                                                <TextBox.Style>
                                                    <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ElementName=EncryptItemTypeComboBox, Path=SelectedValue}" Value="{x:Static encryptPart:EncryptItemType.None}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBox.Style>
                                            </TextBox>
                                            <Button
                                                Grid.Row="3"
                                                Grid.Column="2"
                                                Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}, Path=DataContext.(encryptPart:EncryptViewModel.AttachFileCommand)}"
                                                CommandParameter="{Binding Path=., ValidatesOnNotifyDataErrors=False}"
                                                dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartAttachFileLabel}"
                                                dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_attach_file.png">
                                                <Button.Style>
                                                    <Style TargetType="Button" BasedOn="{StaticResource SmallCommandButton}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding EncryptItemType}" Value="{x:Static encryptPart:EncryptItemType.File}">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>

                                            <!-- id and extensions -->
                                            <Label
                                                Grid.Row="4"
                                                Grid.Column="0"
                                                Content="{x:Static resources:Translation.EncryptPartId}"/>
                                            <Grid
                                                Grid.Row="4"
                                                Grid.Column="1"
                                                Grid.ColumnSpan="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="7*"/>
                                                    <ColumnDefinition Width="1*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox
                                                    Grid.Column="0"
                                                    Text="{Binding ArchiveId}"
                                                    ToolTip="{x:Static resources:Translation.ArchiveIdToolTip}"/>
                                                <TextBox
                                                    Grid.Column="1"
                                                    IsReadOnly="True"
                                                    IsEnabled="False"
                                                    Text="{Binding ArchiveIdExtension}"
                                                    ToolTip="{x:Static resources:Translation.ArchiveIdExtensionToolTip}"/>
                                            </Grid>
                                        </Grid>

                                        <!-- delete item -->
                                        <Button
                                            Grid.Column="1"
                                            Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ItemsControl}, Path=DataContext.(encryptPart:EncryptViewModel.DeleteEncryptItemCommand)}"
                                            CommandParameter="{Binding Path=.,ValidatesOnNotifyDataErrors=False}" 
                                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartDeleteLabel}"
                                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_delete.png"
                                            VerticalAlignment="Top"
                                            Style="{StaticResource SmallCommandButton}"/>
                                    </Grid>
                                </Expander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- buttons -->
                    <StackPanel 
                        Grid.Row="2" 
                        Grid.Column="0" 
                        Orientation="Vertical"
                        VerticalAlignment="Bottom">
                        <Button
                            Command="{Binding AddEncryptItemCommand}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartAddLabel}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_add.png"
                            Style="{StaticResource SmallCommandButton}"/>
                        <Button
                            Command="{Binding CollapseAllCommand}"
                            CommandParameter="{StaticResource True}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartCollapseAll}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_collapse_all.png"
                            Style="{StaticResource SmallCommandButton}"/>
                        <Button
                            Command="{Binding CollapseAllCommand}"
                            CommandParameter="{StaticResource False}"
                            dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartExpandAll}"
                            dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_unfold_more.png"
                            Style="{StaticResource SmallCommandButton}"/>
                    </StackPanel>
                </Grid>
                <!-- buttons -->
                <StackPanel 
                    Grid.Row="5"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
                    <Button
                        Command="{Binding EncryptCommand}"
                        dependencyProperties:TextDependencyProperty.Text="{x:Static resources:Translation.EncryptPartEncrypt}"
                        dependencyProperties:ImageDependencyProperty.Image="../../../Assets/material_symbol_compress.png"
                        Style="{StaticResource SmallCommandButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</mvvm:BaseUserControl>
