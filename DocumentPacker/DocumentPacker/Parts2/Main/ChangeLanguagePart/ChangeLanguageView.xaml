<mvvm:BaseUserControl x:Class="DocumentPacker.Parts2.Main.ChangeLanguagePart.ChangeLanguageView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mvvm="clr-namespace:DocumentPacker.Mvvm">
    <mvvm:BaseUserControl.Resources>
        <Style TargetType="ItemsControl">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Button
                            Command="{Binding   DataContext.ChangeLanguageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding  }"
                            Margin="16" 
                            Padding="16" 
                            VerticalContentAlignment="Stretch">
                            <Grid >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image
                                    Grid.Row="0"
                                    Height="64"
                                    Width="64"
                                    Source="{Binding IconPath}"/>

                                <TextBlock
                                    Grid.Row="1"
                                    FontSize="18"
                                    FontWeight="Bold"
                                    Margin="8"
                                    Text="{Binding Language}"
                                    TextAlignment="Center"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </mvvm:BaseUserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Style="{StaticResource HeadlineTextBlockStyle}"
            Text="{Binding Headline}"/>

        <TextBlock
            Grid.Row="1"
            FontSize="16"
            HorizontalAlignment="Center"
            Margin="16"
            Text="{Binding Description}"
            TextWrapping="Wrap"/>

        <ScrollViewer
            Grid.Row="2"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl
                ItemsSource="{Binding Languages}"
                HorizontalAlignment="Center"/>
        </ScrollViewer>
    </Grid>
</mvvm:BaseUserControl>
