<UserControl x:Class="DocumentPacker.Parts.StartUpPart.View.StartUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:contracts="clr-namespace:DocumentPacker.Parts.StartUpPart.Contracts"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=contracts:IStartUpViewModel, IsDesignTimeCreatable=False}">
    <ItemsControl ItemsSource="{Binding Links, Mode=OneTime}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button 
                    Command="{Binding FollowLinkCommand, Mode=OneTime}"
                    Margin="5"
                    Padding="5">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Name, Mode=OneTime}" />
                        <TextBlock Text="{Binding Description, Mode=OneTime}" />
                    </StackPanel>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>