<UserControl x:Class="DocumentPacker.Views.SubViews.LoadConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!-- new row -->
        <TextBlock
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Grid.Row="0"
            Text="Load Config" />
        <!-- new row -->
        <Label
            Grid.Column="0"
            Grid.Row="1">
            Config File
        </Label>
        <TextBox
            Grid.Column="1"
            Grid.Row="1"
            Text="{Binding ConfigurationFileName}" />
        <Button
            Grid.Column="2"
            Grid.Row="1">
            ...
        </Button>
        <!-- new row -->
        <Button
            Grid.Column="2"
            Grid.Row="2"
            Command="{Binding }">
            Load
        </Button>
    </Grid>
</UserControl>