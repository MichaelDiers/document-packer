<UserControl x:Class="DocumentPacker.Views.FatalErrorMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Panel.ZIndex="1000">
    <Border BorderBrush="Black"
            BorderThickness="1"
            Background="#80000000">
        <Border MaxWidth="400"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="AliceBlue"
                Padding="20px"
                CornerRadius="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" TextWrapping="Wrap" Text="{Binding FatalErrorMessage}" />
                <Button Grid.Row="1" Command="{Binding CloseFatalErrorMessageCommand}">OK</Button>
            </Grid>
        </Border>
    </Border>
</UserControl>